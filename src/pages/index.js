import React from 'react';
import clsx from 'clsx';
import Layout from '@theme/Layout';
import Link from '@docusaurus/Link';
import useDocusaurusContext from '@docusaurus/useDocusaurusContext';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from './styles.module.css';

const features = [
  {
    title: <>Acerca de Yaipan</>,
    imageUrl: 'img/undraw_docusaurus_react.svg',
    description: (
      <>
        Nos especializamos en el desarrollo de sistemas de información combinando
        la investigación e innovación.
      </>
    ),
  },
  {
    title: <>Documentación Técnica</>,
    imageUrl: 'img/undraw_docusaurus_tree.svg',
    description: (
      <>
        Le proporcionamos la documentación técnica que necesita para poder
        integrarse con nuestros web services.
      </>
    ),
  },
  {
    title: <>Instalación de Web Services</>,
    imageUrl: 'img/undraw_docusaurus_mountain.svg',
    description: (
      <>
        Comuníquese con nosotros si desea que se instalen algunos de los web
        services que encuentra en esta página.
      </>
    ),
  },
];

function Feature({imageUrl, title, description}) {
  const imgUrl = useBaseUrl(imageUrl);
  return (
    <div className={clsx('col col--4', styles.feature)}>
      {imgUrl && (
        <div className="text--center">
          <img className={styles.featureImage} src={imgUrl} alt={title} />
        </div>
      )}
      <h3>{title}</h3>
      <p>{description}</p>
    </div>
  );
}

function Home() {
  const context = useDocusaurusContext();
  const {siteConfig = {}} = context;
  return (
    <Layout
      title={`Docs`}
      description="Description will go into a meta tag in <head />">
      <header className={clsx('hero hero--primary', styles.heroBanner)}>
        <div className="container">
          <h1 className="hero__title">{siteConfig.title}</h1>
          <p className="hero__subtitle">{siteConfig.tagline}</p>
          <div className={styles.buttons}>
            <Link
              className={clsx(
                'button button--outline button--secondary button--lg',
                styles.getStarted,
              )}
              to={useBaseUrl('docs/')}>
              Ingrese
            </Link>
          </div>
        </div>
      </header>
      <main>
        {features && features.length > 0 && (
          <section className={styles.features}>
            <div className="container">
              <div className="row">
                {features.map((props, idx) => (
                  <Feature key={idx} {...props} />
                ))}
              </div>
            </div>
          </section>
        )}
      </main>
    </Layout>
  );
}

export default Home;
